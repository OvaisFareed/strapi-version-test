'use strict';

/**
 * tools-live-exchange-rate service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::tools-live-exchange-rate.tools-live-exchange-rate');
