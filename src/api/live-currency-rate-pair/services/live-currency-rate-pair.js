'use strict';

/**
 * live-currency-rate-pair service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::live-currency-rate-pair.live-currency-rate-pair');
