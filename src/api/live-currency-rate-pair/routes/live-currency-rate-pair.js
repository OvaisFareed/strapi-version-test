'use strict';

/**
 * live-currency-rate-pair router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::live-currency-rate-pair.live-currency-rate-pair');
