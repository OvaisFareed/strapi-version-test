'use strict';

/**
 * live-exchange-rates-default router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::live-exchange-rates-default.live-exchange-rates-default');
