'use strict';

/**
 * live-exchange-rates-default service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::live-exchange-rates-default.live-exchange-rates-default');
