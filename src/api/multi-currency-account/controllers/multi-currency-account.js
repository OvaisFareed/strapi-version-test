'use strict';

/**
 * multi-currency-account controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::multi-currency-account.multi-currency-account');
