'use strict';

/**
 * multi-currency-account service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::multi-currency-account.multi-currency-account');
