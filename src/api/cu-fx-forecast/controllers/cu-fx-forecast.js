'use strict';

/**
 * cu-fx-forecast controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::cu-fx-forecast.cu-fx-forecast');
