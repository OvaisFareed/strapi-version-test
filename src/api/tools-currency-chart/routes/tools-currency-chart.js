'use strict';

/**
 * tools-currency-chart router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::tools-currency-chart.tools-currency-chart');
