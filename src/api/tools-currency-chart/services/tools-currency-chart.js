'use strict';

/**
 * tools-currency-chart service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::tools-currency-chart.tools-currency-chart');
