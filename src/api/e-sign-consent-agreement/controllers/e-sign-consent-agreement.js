'use strict';

/**
 * e-sign-consent-agreement controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::e-sign-consent-agreement.e-sign-consent-agreement');
